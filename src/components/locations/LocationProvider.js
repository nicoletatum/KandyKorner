import React, { useState, createContext } from "react"

export const LocationContext = createContext()

export const LocationProvier = (props) => {
    const [locations, setLocations] = useState([])

    const getLocations = ()
}